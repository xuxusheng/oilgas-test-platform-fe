name: 油气测试平台前端 CI

on:
  # 分支推送触发
  push:
    branches:
      - main
      - develop
    tags:
      - 'v*'
      - '*.*.*'

  # Pull Request 触发
  pull_request:
    branches:
      - main
      - develop

# 并发控制，防止多个构建同时进行
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ==================== 区域 1: 通用检查任务 ====================
  # 代码质量检查
  code-quality:
    name: 代码质量检查
    runs-on: ubuntu-latest

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 安装 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: 设置 Node.js 环境
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 安装依赖
        run: pnpm install --frozen-lockfile

      - name: 代码格式化检查
        run: pnpm format:check

      - name: ESLint 代码检查
        run: pnpm lint

      - name: TypeScript 类型检查
        run: pnpm typecheck

  # 构建验证
  build-verify:
    name: 构建验证
    runs-on: ubuntu-latest
    needs: code-quality

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 安装 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: 设置 Node.js 环境
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 安装依赖
        run: pnpm install --frozen-lockfile

      - name: 构建项目
        run: pnpm build

      - name: 验证构建产物
        run: |
          if [ -d "dist" ]; then
            echo "✅ 构建成功，dist 目录已生成"
            echo "📦 构建产物大小:"
            du -sh dist/
            echo "📄 文件数量:"
            find dist/ -type f | wc -l
          else
            echo "❌ 构建失败，dist 目录不存在"
            exit 1
          fi

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ github.run_number }}
          path: dist/
          retention-days: 7

  # ==================== 区域 2: 开发环境任务 ====================
  # 开发环境镜像构建和推送
  build-and-push-dev:
    name: 开发环境 - 镜像构建推送
    runs-on: ubuntu-latest
    needs: build-verify
    if: github.ref == 'refs/heads/develop'

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 提取元数据（镜像标签）
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ vars.DOCKER_IMAGE }}
          tags: |
            # 开发环境使用分支名和短 SHA
            type=ref,event=branch,prefix=dev-
            type=sha,prefix=dev-,format=short
            type=raw,value=dev-latest,enable=${{ github.ref == 'refs/heads/develop' }}
          labels: |
            org.opencontainers.image.title=${{ github.event.repository.name }}
            org.opencontainers.image.description=油气测试平台前端应用
            org.opencontainers.image.vendor=油气测试团队

      - name: 设置 QEMU
        uses: docker/setup-qemu-action@v3

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录 Docker 镜像仓库
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.DOCKER_IMAGE }}
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 构建并推送镜像
        id: docker_build
        uses: docker/build-push-action@v6
        with:
          context: .
          cache-from: type=gha
          cache-to: type=gha,mode=max
          file: ./ci/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: 构建成功，打印信息
        run: |
          echo "✅ 镜像构建并推送成功"
          echo "📦 镜像: ${{ steps.meta.outputs.tags }}"
          echo "🔍 摘要: ${{ steps.docker_build.outputs.digest }}"
          echo "🏃 运行编号: ${{ github.run_number }}"
          echo "🔗 工作流: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo "🚀 镜像已推送到仓库，可手动部署到开发环境"

  # ==================== 区域 3: 生产环境任务 ====================
  # 生产环境镜像构建和推送
  build-and-push-prod:
    name: 生产环境 - 镜像构建推送
    runs-on: ubuntu-latest
    needs: build-verify
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 提取元数据（镜像标签）
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ vars.DOCKER_IMAGE }}
          tags: |
            # 生产环境使用版本号和短 SHA
            type=ref,event=branch,prefix=prod-
            type=sha,prefix=prod-,format=short
            type=raw,value=prod-latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
          labels: |
            org.opencontainers.image.title=${{ github.event.repository.name }}
            org.opencontainers.image.description=油气测试平台前端应用（生产环境）
            org.opencontainers.image.vendor=油气测试团队

      - name: 设置 QEMU
        uses: docker/setup-qemu-action@v3

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录 Docker 镜像仓库
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.DOCKER_IMAGE }}
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 构建并推送镜像
        id: docker_build
        uses: docker/build-push-action@v6
        with:
          context: .
          cache-from: type=gha
          cache-to: type=gha,mode=max
          file: ./ci/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: 构建成功，打印信息
        run: |
          echo "✅ 镜像构建并推送成功"
          echo "📦 镜像: ${{ steps.meta.outputs.tags }}"
          echo "🔍 摘要: ${{ steps.docker_build.outputs.digest }}"
          echo "🏃 运行编号: ${{ github.run_number }}"
          echo "🔗 工作流: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo "🚀 镜像已推送到仓库，可手动部署到生产环境"

  # ==================== 区域 4: PR 检查任务 ====================
  # Pull Request 检查
  pr-check:
    name: Pull Request 检查
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 安装 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: 设置 Node.js 环境
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 安装依赖
        run: pnpm install --frozen-lockfile

      - name: 代码质量检查
        run: |
          pnpm format:check
          pnpm lint
          pnpm typecheck

      - name: 构建验证
        run: pnpm build

      - name: 验证构建产物
        run: |
          if [ -d "dist" ]; then
            echo "✅ 构建成功"
            echo "📦 构建产物大小: $(du -sh dist/ | cut -f1)"
          else
            echo "❌ 构建失败"
            exit 1
          fi

      - name: 检查完成
        run: |
          echo "🎉 PR 检查全部通过！"
          echo "🔍 分支: ${{ github.head_ref }}"
          echo "📝 标题: ${{ github.event.pull_request.title }}"
