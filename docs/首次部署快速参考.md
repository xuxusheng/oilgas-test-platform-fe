# 首次部署功能 - 快速参考

## 📋 文件清单

### ✅ 新增文件（3个）

1. `src/pages/InitAdmin/index.tsx` - 初始化管理员页面
2. `src/components/RouteGuard.tsx` - 路由守卫（增强版）
3. `src/features/auth/utils.ts` - 认证工具函数

### ✅ 修改文件（5个）

1. `src/router/routes.ts` - 新增 INIT_ADMIN 路由常量
2. `src/router/router.tsx` - 更新路由配置
3. `src/pages/Login/index.tsx` - 增强系统状态检测
4. `src/pages/Register/index.tsx` - 增强系统状态检测
5. `src/features/auth/index.ts` - 导出工具函数

## 🎯 核心代码示例

### 路由配置

```typescript
// src/router/router.tsx
const router = createBrowserRouter([
  {
    path: ROUTES.INIT_ADMIN,
    element: <RouteGuard requireInit={true}><InitAdmin /></RouteGuard>,
  },
  {
    path: ROUTES.LOGIN,
    element: <RouteGuard requireAuth={false}><Login /></RouteGuard>,
  },
  {
    path: '/',
    element: <RouteGuard requireAuth={true}><BasicLayout /></RouteGuard>,
    children: [/* ... */],
  },
])
```

### API 调用（已存在）

```typescript
// 检测系统状态
const { data, isLoading, error } = useSystemStatus()

// 创建管理员
const { mutate, isPending } = useInitAdmin()
mutate({ username: 'admin', password: 'xxx' })
```

## 🔍 故障排除

### 问题1: useNavigate 错误

**原因**: 组件不在 Router 上下文中
**解决**: 确保组件被 `<RouterProvider>` 或 `<RouteGuard>` 包裹

### 问题2: API 路径错误

**原因**: 重复 `/api` 前缀
**解决**: API 路径以 `/` 开头，不包含 `/api`

### 问题3: 类型错误

**原因**: API 响应结构不匹配
**解决**: 使用 `response.data.data.xxx` 访问数据

## ✅ 验证清单

- [ ] 所有文件创建/修改完成
- [ ] TypeScript 类型检查通过
- [ ] ESLint 代码规范通过
- [ ] Prettier 格式化通过
- [ ] 后端接口可用
- [ ] 本地测试通过

## 🚀 下一步

1. **测试**: 运行 `pnpm dev` 并访问应用
2. **验证**: 检查首次部署流程是否正常
3. **构建**: 运行 `pnpm build` 生成生产包
4. **部署**: 配置 Nginx 反向代理

---

**完成！** 所有代码已就绪，可以直接使用 🎉
